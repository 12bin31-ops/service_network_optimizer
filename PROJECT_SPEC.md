# PROJECT_SPEC — Service Network Optimizer

설계 문서. 구현 판단의 근거와, 다음에 붙일 것을 남긴다.

---

## 1. 문제 정의

### 현상
완성차 국내사업의 서비스 네트워크(정비 거점)는 과거 판매 실적을 따라 배치된다. 그러나 정비 수요를 결정하는 것은 판매량이 아니라 운행 중인 차량의 차령 구조다. 시간이 지나면서 두 분포가 어긋나고, 다음 두 가지 구조적 빈틈이 생긴다.

- **접근성 갭**: 30분 내 도달 가능한 거점이 없는 지역
- **용량 갭**: 거점은 있으나 처리 능력을 초과해 대기가 발생하는 상권

거점이 있는 곳에서도 문제는 남는다. 불만이 많은 거점은 **붐벼서**일 수도, **운영이 약해서**일 수도 있고, 보증 연장 같은 대고객 정책은 공식망 유입을 늘려 **용량 갭을 키운다.**

### 산출 목표
1. 시군구별 연간 정비 입고 수요 추정치 (보증수리 / 유상 분해)
2. 두 갭 유형을 분리한 진단표와 0~100 갭 스코어
3. 거점별 품질 등급과 품질 저하 원인(과부하형 / 운영형) 판정
4. 예산 제약 하의 최적 신설 조합, 그리고 신설 · 증설을 함께 고른 최적 투자안
5. 공식망 유입률 정책 시나리오의 네트워크 영향 (누수율 · 과부하 상권)
6. 임원 보고용 한국어 리포트, 지도, 대시보드

### 비목표
- 개별 부지 선정, 임대료·계약 제약 반영
- 실시간 정비 예약/입고 시스템 연동
- 차종·정비 항목 단위 세분화

---

## 2. 데이터 설계

### 분석 단위
시군구(83개 표본). 읍면동은 등록 통계의 공개 단위가 아니고, 시도는 서비스망 의사결정 단위로 너무 굵다.

### 표준 프레임
수집 모드(`sample` / `live`)와 무관하게 분석 로직이 소비하는 세 프레임.

| 프레임 | 컬럼 |
|---|---|
| `regions` | sigungu_code, sido, sigungu, lat, lon, urban_class, population |
| `vehicle_parc` | sigungu_code, age_bucket, vehicles, ev_vehicles, source |
| `service_centers` | center_id, name, center_type, sigungu_code, lat, lon, bays, source |
| `center_voc` (선택) | center_id, period, voc_per_1k_jobs, comeback_rate, source |

`age_bucket ∈ {0-2, 3-5, 6-10, 11+}`, `urban_class ∈ {metro, city, rural}`, `center_type ∈ {bluehands, hitech}`.

### 수집 모드
- **sample** — 공개 참조표(`data/reference/sigungu.csv`) 위에서 규칙 기반 합성 모수를 생성. 시드 고정으로 재현 가능. 저장소가 API 키 없이 항상 완주하게 하는 것이 목적이며, 동시에 검출 대상 현상(지방 노후 차량 집중, 판매 실적 기반 거점 배치, 부하형 · 운영형 품질 저하)을 의도적으로 주입한다. VOC 는 거점망 난수 소비가 끝난 뒤 뽑아, VOC 규칙을 바꿔도 거점망은 그대로다.
- **live** — 사람이 내려받아 `data/raw/` 에 둔 공공 통계를 표준 프레임으로 정규화. 크롤링하지 않는다(배포 형식 변동·이용약관).

### 저장
SQLite. 단계 산출물을 테이블로 물질화해 (a) 단계별 독립 재실행, (b) 대시보드·에이전트의 단일 소스 참조를 보장한다. 스키마는 `src/snx/storage/schema.sql`.

```
regions ─┬─ vehicle_parc
         ├─ service_centers ── center_voc
         ├─ demand            [1 수요]
         ├─ coverage          [2 커버리지]
         ├─ gap_scores        [3 갭]
         ├─ center_quality    [3' 품질]
         ├─ siting_results    [4 신설]  ── siting_runs
         └─ plan_actions      [4' 투자] ── plan_runs
                                           reports [5]
```

새 `ingest` 는 하류 테이블을 외래키 역순으로 모두 비운다 — 수집이 바뀌면 아래 산출물은 전부 무효다.

---

## 3. 모형

### 3.1 수요 추정

```
D_i = Σ_a N_ia^ice · r_a · λ_a  +  Σ_a N_ia^ev · r_a · λ_a · m
```

| 기호 | 의미 | 기본값 |
|---|---|---|
| `r_a` | 차령버킷 a 의 연간 정비 입고 원단위 (회/대/년) | 1.6 / 1.9 / 2.3 / 2.6 |
| `λ_a` | 공식 서비스망 유입률 | 0.85 / 0.62 / 0.38 / 0.18 |
| `m` | 전기차 정비 빈도 배수 | 0.55 |

**설계 판단**: 차령이 오르면 `r_a` 는 커지지만 `λ_a` 가 더 빠르게 떨어진다. 이 비선형성이 "인구 대비 수요가 과대/과소한 지역"을 만들고, 인구 기반 배치가 놓치는 지점이 여기다. 전기차는 고전압 정비가 하이테크센터로 흡수되는 비율(`ev_hitech_ratio`)을 따로 뽑아 둔다 — 향후 하이테크센터 입지를 별도 최적화할 때 쓴다.

**보증수리 분해**: `W_i = Σ_a ice_ia · ω_a + Σ_a ev_ia · ω^ev_a`. 일반부품 3년 · 동력계통 5년 보증을 버킷 단위로 근사하고(0.45 / 0.22 / 0.03 / 0), 전기차는 배터리 10년 보증으로 6-10 버킷에도 비중을 남긴다(0.55 / 0.40 / 0.25 / 0). 정기점검 · 소모품은 보증기간 내에도 유상이므로 0-2 버킷도 1.0 이 아니다. 지역별 보증 비용 부담과 유상 정비 매출 구조가 갈리는 지점을 보여 준다.

### 3.2 접근성

```
t_ij = haversine(i, j) × 1.30 ÷ speed(urban_class_i) × 60
```

속도는 출발지(수요 지역)의 도시등급을 따른다 — 통행 대부분이 거주지 주변 도로망에서 발생하기 때문. metro 26 / city 38 / rural 52 km/h.

**대안과 기각 사유**: OSRM·T-Map 라우팅은 정확하지만 (a) 전국 시군구 × 수백 거점 반복 계산에 API 비용/시간이 들고, (b) 외부 서비스에 재현성이 묶인다. 근사 오차보다 재현성이 중요하다고 판단했다. 교체 지점은 `coverage/distance.travel_time_matrix` 함수 하나로 격리했다.

### 3.3 수요 분배 (Huff)

```
w_ij ∝ max(t_ij, 5분)^(-1.4)      단 t_ij ≤ 30분,  Σ_j w_ij = 1
```

**최근접 100% 배정을 기각한 이유**: 거점이 촘촘한 대도시에서 특정 거점의 부하가 비현실적으로 튄다. **5분 하한을 둔 이유**: 지역을 하나의 점으로 다루므로 같은 시군구 내 거점 간 소요시간이 0 에 가까워지면 한 곳으로 쏠린다. 실제 수요는 지역 내부에 면적으로 퍼져 있다.

감쇠계수 1.4 는 전국 미충족 비율이 합리적 구간(10~20%)에 들어오도록 보정한 값이다. 실적 데이터가 있으면 실제 입고 분포로 캘리브레이션해야 한다. — **알려진 튜닝 포인트**

### 3.4 갭 분해

거점 j: `healthy_j = capacity_j × 0.85`, `overflow_ratio_j = max(0, assigned_j − healthy_j) / assigned_j`

```
uncovered_i = D_i × (1 − Σ_j w_ij)                  접근성 갭
overflow_i  = D_i × Σ_j w_ij · overflow_ratio_j     용량 갭
covered_i   = D_i − uncovered_i − overflow_i
```

### 3.5 갭 스코어

세 축을 0~1 로 정규화해 가중합 × 100.

| 축 | 정규화 | 가중치 |
|---|---|---|
| 미충족 수요 (`uncovered + overflow`) | log1p 후 min-max (두터운 꼬리) | 0.50 |
| 최근접 거점 소요시간 | 기준시간 2배에서 포화 후 min-max | 0.25 |
| 건전 부하율 초과분 | min-max | 0.25 |

가중치 합이 1 이 아니면 실행을 거부한다.

### 3.6 입지 최적화 (Capacitated MCLP)

```
max    Σ_ij v_ij · z_ij           v_ij = 1 + 0.35 · (1 − t_ij/T)
s.t.   Σ_j z_ij ≤ unmet_i    ∀i
       Σ_i z_ij ≤ C · x_j    ∀j
       z_ij = 0 if t_ij > T
       Σ_j x_j ≤ p
       x_j ∈ {0,1},  z_ij ≥ 0
```

후보지 = 갭 스코어 상위 60개 시군구 중심점. 솔버 CBC.

**접근성 가중 `v_ij` 를 넣은 이유**: 순수 최대커버링은 용량이 binding 일 때 모든 후보가 동률(= 설계용량 만큼 흡수)이 되어 우선순위 변별력이 사라진다. 같은 양을 흡수해도 더 가까이서 흡수하는 배치를 선호하게 해 축퇴를 깬다. 보고 지표(`objective_value`)는 가중치를 뺀 실제 흡수 수요를 쓴다.

**동률 해소항**: 용량이 binding 이고 각 후보가 자기 지역 수요만으로 설계용량을 채우면 흡수량 · 접근성 가중치까지 모두 같아진다. 이때만 작동하도록 `ε · gap_score/100 · x_j` (ε = 1e-3) 를 더해, 동률이면 갭 스코어가 높은 후보를 연다. ε 가 흡수 1건의 가중치(≥ 1)보다 충분히 작아 흡수량과 교환되지 않는다.

**이 모형이 실제로 만들어내는 결론**: 갭 상위 지역이 신설 대상에서 빠지는 경우가 나온다. 미충족 수요가 거점 설계용량(6베이 = 9,570건/년)의 절반에 못 미치면 신설은 용량 과잉이고, 증설이나 협력 정비소 제휴가 옳은 처방이다. 갭 순위대로 짓는 것과 포트폴리오 최적해가 다르다는 것 — 3.8 의 투자안 모형이 이 처방을 정량화한다.

### 3.7 거점 품질 진단

```
wait_j = base · (1 + ρ_j / (1 − ρ_j)),  ρ_j ≤ 0.98,  wait_j ≤ 14일
risk_j = 100 · (0.40 · mm(wait) + 0.35 · mm(VOC) + 0.25 · mm(재입고))     mm = min-max
```

**대기행렬 근사를 쓴 이유**: 부하율을 선형으로 쓰면 80% 와 95% 거점의 차이가 15%p 로 보이지만, 체감 대기는 몇 배 차이 난다. M/M/1 형태의 `ρ/(1−ρ)` 가 이 비선형성을 가장 단순하게 담는다. 절대 대기일수는 근사치이며 순위를 매기는 용도로만 쓴다.

**원인 분리**: 부하 초과(`ρ > 0.85`) × 불만 신호(VOC · 재입고율 백분위 평균이 상위 15%) 의 2×2.

| | 불만 신호 | 없음 |
|---|---|---|
| 부하 초과 | 과부하형 → 증설 · 예약 분산 | 대기 리스크 → 예방적 증설 |
| 정상 부하 | 운영형 → 품질 점검 · 기술 교육 | 정상 |

불만 신호를 두 지표의 **백분위 평균**으로 둔 이유: 한 지표만 튀는 우연(소규모 거점의 VOC 몇 건)보다 VOC 와 재입고가 함께 나쁜 거점을 잡기 위해서다. 동점은 `rank(method="min")` 으로 처리해 전원이 같은 값이면 아무도 신호가 되지 않는다. VOC 가 없는 live 환경에서는 없는 축을 빼고 가중치를 재정규화한다.

### 3.8 신설 + 증설 동시 최적화

```
변수   x_k ∈ {0,1}   y_j ∈ {0..4} (정수)   z_ik ≥ 0   0 ≤ e_ij ≤ o_ij
max    Σ v_ik z_ik + Σ v_ij e_ij + ε Σ g_k x_k
s.t.   Σ_k z_ik + Σ_j e_ij ≤ unmet_i            ∀i
       Σ_i z_ik ≤ C · x_k                        ∀k    C = 6베이 설계용량
       Σ_i e_ij ≤ h · y_j                        ∀j    h = 1베이 × 건전 부하율
       F · Σ x_k + c · Σ y_j ≤ B
o_ij = D_i · w_ij · overflow_ratio_j
```

기본 단가: 신설 F = 30억(6베이, 부지 제외), 증설 c = 2.5억/베이, 거점당 최대 +4베이, 예산 B = 450억(신설 15개소 규모).

**증설 흡수 상한을 `o_ij` 로 둔 이유**: 증설은 이미 그 거점에 배정된 수요 중 대기로 남은 몫만 풀 수 있다. 30분 밖 수요(접근성 갭)나 다른 거점의 대기는 풀 수 없다. `o_ij` 를 커버리지 진단의 `Assignment` 에서 그대로 꺼내 쓰므로 진단표와 최적화의 숫자가 어긋나지 않는다.

**증설 1베이 용량을 건전 부하율로 할인한 이유**: 대기는 건전 부하율 기준으로 정의했으므로, 대기를 해소하는 증설 용량도 같은 기준이어야 한다. 신설 거점은 3.6 의 기존 정의(설계용량 100%)를 유지했다 — 결과적으로 신설에 약간 유리한 비교다.

**비교 기준**: 같은 예산에서 `신설만`(= 3.6 모형, p = ⌊B/F⌋) · `증설만` · `최적 조합` 을 함께 보고한다. 최적 조합은 두 단일 수단 해를 포함하므로 이론상 둘보다 작을 수 없다(MIP 상대 갭 0.1% 이내). 리포트는 0.5× / 1× / 2× 예산 민감도와, 예산 무제한일 때 증설만으로 흡수 가능한 상한을 함께 싣는다.

**sample 결과가 말하는 것**: 450억에서 최적 조합은 신설 0 · 증설 71곳(+180베이)으로 신설만 대비 1.70배를 흡수한다. 증설 상한은 미충족의 81% 이고, 그 너머(30분 밖 수요 · 거점당 증설 한도를 넘는 집중 과부하)부터 신설이 선택된다. 단가 비율이 바뀌면 구성이 바뀐다 — `test_expensive_expansion_shifts_to_new_sites` 가 이 방향성을 검증한다.

**알려진 계산 특성**: 예산이 증설 상한 근처(≈1,300~1,600억)에 오면 신설 · 증설 조합이 대칭적으로 많아져 CBC 가 수십 초씩 걸린다. 기본 예산에서는 1초 미만이다.

### 3.9 정책 시나리오

유입률 `λ_a` 만 바꾼 설정으로 3.1 → 3.3 → 3.4 를 메모리에서 다시 계산해 기준선과 비교한다.

```
누수율 = Δunmet / Δvisits      (늘어난 입고 중 서비스망이 받지 못하는 비율)
```

DB 를 쓰지 않는 이유: 시나리오는 대시보드 슬라이더로 수십 번 돌려 보는 도구이며, 기준선 산출물을 오염시키면 안 된다. 기본 시나리오(6-10 버킷 0.38 → 0.50)는 리포트에 싣는다.

---

## 4. 에이전트 설계

CrewAI 순차 프로세스 3단계.

| 에이전트 | 역할 | 산출 |
|---|---|---|
| 수요 애널리스트 | 갭 상위 지역이 왜 상위인지 차령·모수·접근성·부하율로 규명 | 지역별 진단 노트 |
| 서비스망 전략가 | 갭 유형별 처방 분리, 신설/증설/운영개선 구분, 정책 시나리오 함의 판정 | 전략 판정 노트 |
| 보고서 작성자 | 임원 보고용 한국어 마크다운 1페이지 | 최종 리포트 |

역할·목표·배경은 `agents/prompts/agents.yaml`, 태스크는 `tasks.yaml` 에 분리했다. 프롬프트 수정이 코드 변경이 되지 않게 한다.

### 핵심 설계 원칙 — 숫자는 도구가, 서술은 에이전트가

도구 8종(`national_summary`, `top_gap_regions`, `region_detail`, `siting_plan`, `quality_watchlist`, `investment_plan`, `policy_scenario`, `methodology_notes`)은 전부 SQLite 를 읽어 JSON 을 반환한다. 에이전트에게 계산 능력을 주지 않고, 프롬프트에도 "도구 결과에서만 인용한다"를 명시한다. LLM 이 수치를 지어내는 경로 자체를 차단하는 구조다.

도구 함수는 CrewAI 와 무관한 순수 함수로 두고 크루 구성 시점에만 `@tool` 로 감싼다 — CrewAI 미설치 환경에서도 테스트가 그대로 돈다.

### 폴백
`crewai` 미설치 또는 `OPENAI_API_KEY` 부재 또는 에이전트 실행 실패 시, 결정론적 템플릿 리포트를 반환하고 경고만 남긴다. 리포트 생성 실패가 파이프라인 전체를 막지 않는다.

---

## 5. 구현 순서 (완료)

| 단계 | 내용 | 검증 |
|---|---|---|
| 1 | 설정 스키마 · SQLite 저장소 | 스키마 멱등성 |
| 2 | 참조표 · sample 생성기 · live 로더 | 시드 결정성, 지방 노후차 비중 > 대도시 |
| 3 | 수요 추정 | 폐형해 일치, 전기차 배수, 버킷 검증 |
| 4 | 거리 · 커버리지 · 갭 스코어 | 서울-부산 실거리, Huff 정규화, 무거점/원거리/과부하 경계조건 |
| 5 | 입지 최적화 | 예산 제약, 흡수량 상한, 거점 수 단조성 |
| 6 | 리포트 · 에이전트 · 지도 · 대시보드 | JSON 유효성, 폴백 동작, 지도 렌더링 |
| 7 | 통합 | 전 테이블 적재, 지역 정합성, 미충족 비율 합리성 |
| 8 | 보증수리 분해 | 폐형해, 보증+유상 = 전체, 배터리 보증 |
| 9 | 거점 VOC · 품질 진단 | 2×2 원인 분리, VOC 부재 폴백, 대기 비선형성, 부하-VOC 상관 |
| 10 | 신설 + 증설 최적화 | 예산 · 증설 한도, 혼합안 ≥ 단일 수단, 단가 변화 시 신설 전환, 예산 단조성 |
| 11 | 정책 시나리오 | 입력 검증, 무변화 = 무영향, DB 비오염 |

64 tests · ruff 통과.

---

## 6. 다음에 붙일 것

우선순위 순.

1. **라우팅 API 연동** — `travel_time_matrix` 만 교체. 산악·도서 지역 접근성 과대평가 해소. 영향 가장 큼.
2. **실적 캘리브레이션** — 실제 입고 · 보증 청구 데이터로 `r_a`, `λ_a`, `ω_a`, Huff 감쇠계수를 추정. 현재는 시나리오 가정값.
3. **시계열 수요 예측** — 차령 코호트를 연도별로 굴려 3~5년 후 수요 분포를 예측. 전기차 전환에 따른 정비 수요 이동이 핵심 변수. 투자안 모형을 다기간(연도별 예산)으로 확장하는 전제.
4. **하이테크센터 별도 입지** — `hitech_visits` 는 이미 계산해 두었으나 최적화에는 미반영. 고전압 정비는 접근 기준 시간이 다르다(광역 단위).
5. **VOC 텍스트 분류** — 지금은 VOC 건수만 쓴다. 불만 텍스트를 대기 · 수리품질 · 응대 · 비용으로 분류하면 3.7 의 원인 분리를 수치 규칙이 아닌 고객 발화로 교차 검증할 수 있다.
6. **정책 · 투자 동시 최적화** — 3.9 의 누수를 3.8 의 제약으로 넣어 "정책 목표 유입률을 달성하는 최소 네트워크 투자"를 역산.

## 7. 알려진 한계

- 소요시간 근사로 인한 지형 미반영
- 정비 원단위·유입률의 시나리오 가정 의존
- 후보지 = 시군구 중심점 (부지 제약 미반영)
- 정적 수요 (시계열 없음)
- 브랜드 점유율을 전국 단일값으로 적용 (지역별 편차 미반영)
- 신설 · 증설 단가 가정 의존 — 투자안 구성은 단가 비율에 민감
- sample VOC 는 부하 대리변수 + 운영 역량 잡음으로 만든 합성값 (원인 분리 로직 검증용)
