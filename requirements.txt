# 런타임 의존성 — 개발 설치는 `pip install -e ".[dev]"` 를 권장 (pyproject.toml 기준)
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
pydantic>=2.5
typer>=0.12
rich>=13.0
pulp>=2.8,<4.0
scikit-learn>=1.3
folium>=0.15
streamlit>=1.30
plotly>=5.18
